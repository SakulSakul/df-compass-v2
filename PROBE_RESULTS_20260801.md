# 단계 ⓪ 프로브 결과 (구현 지시서 v1 — 읽기 전용, 코드 무수정)

> 실행일 2026-08-01 (지시서 발행 07-30, 세션 재개 시점 실측 — 날짜는 기계
> 시각 기준으로 적는다). SELECT/RPC 전용 · DB 쓰기 0 · 엔진 코드 무수정.
> **wave 1~3 착수하지 않음 — 감리자 승인 대기.**

---

## ⓪-2 성립 확인 3종 (갈림길 먼저)

### (i) conf 소비처 — **표시·기록 전용. action 입력 아님** ✅
코드 경로 증명 (`compass_engine/verify.py`):
```
39  if n_dm > 0:      action = "block"     # 카운트에서 직접 결정
41  elif n_am > 0:    action = "degrade"
43  else:             action = "pass"
46  denom = n_ok + n_am + n_dm             # ← action 확정 '이후' 산출
47  confidence = (n_ok / denom) if denom else 0.0
```
`action` 은 `n_dm/n_am` 카운트만으로 확정되고, `confidence` 는 그 뒤에
독립 계산된다 — **confidence 가 action 분기에 입력되는 경로 없음**.

소비처 전수 (grep `confidence`, `__pycache__` 제외):
| 위치 | 성격 |
|---|---|
| `app.py:388` | stderr `[ui:trace]` 문자열 (화면 미표출 — UI 정비 때 칩 제거) |
| `eval/compare_v1v2.py:100` | 결과 JSON 기록 |
| `tests/test_verify.py` 3곳 | 단위 테스트 어서션 |
| `stages.py:89/97` | 타입 정의(VerifyResult.confidence / RenderResult.confidence_chip) |
- `RenderResult.confidence_chip` 은 **타입 정의만 존재, 구현 소비처 0**.
- v1port 의 `confidence` 는 critical 분류기 자체 값(별개 계통, ≥0.66 게이트).

**→ 지시서 wave 2 conf 항의 정지 조건(“action 입력으로 판명”) 미해당.
서수 4등급 전환 진행 가능.** 부수: 전환 시 소비처 3곳(app trace·하니스
기록·test_verify 어서션 3건) 동반 수정이 불가피 — 구현 범위 내로 보고.

### (ii) 원장 물리 소재 — **DB 테이블 아님. 런타임 파생** ✅
`registry.load_ledger()` 는 `nexus_documents(id,title,status=active)` +
`nexus_chunks(document_id,text)` 페이지 SELECT 후 `build_ledger()` 로
메모리 객체 생성. 별도 원장 테이블·캐시 파일 없음 (앱은
`st.cache_resource` 로 기동 1회 캐시). → **wave 2 앵커 사전은 DB 쓰기 없이
registry 파생 함수로 도출 가능** (ADR-8 무저촉).

### (iii) 청크 메타 조번호 — **존재하나 희소·부분 부정확** ⚠️
| 항목 | 실측 |
|---|---|
| `nexus_chunks.article_no` 물리 컬럼 | 실재 ✅ |
| non-null 커버리지 | **123/774 (15.9%)** |
| 본문 첫 조번호와 불일치 | 5/115 표본 (4.3%) |
| RPC(`v4_ctx`) 반환 키 | `article_no` 포함, 단 표본 5건 전부 `None` |
**→ 투입 청크의 “실재 조번호” 판정 근거로 단독 사용 불가.** wave 2 는
심의 확정대로 **원장(text 재추출) 파생 사전**을 정본으로 쓰고, 청크
`article_no` 는 보조 신호로만 (기존 v1_retriever 주석의 “조의N 붕괴
가능성” 경고와 정합).

---

## ⓪-1 앵커 0 재파싱 (독립 정규식 — `citations.py` 미사용)

방법: 하니스 89건 중 **기록 앵커 0인 52건**(golden 33 + variant 19)의 답변
원문에 독립 구현(`제\s*(\d+)\s*조(의\d+)?` + 원장 101 문서명 exact/접두어
생략 매칭 + 근접 규칙)을 재적용해 기록과 diff.

| 분류 | 건수 | golden/variant | id |
|---|---|---|---|
| (a) 추출기 실패 후보 | **2** | 1 / 1 | g27, g27v1 |
| (b) 문서수준 인용 | **45** | 27 / 18 | g01·g04·g05·g06·g07·g08·g09·g11·g14~g19·g22·g24·g29~g32·g34~g36·g41·g43·g45·g51 + 변형 18 |
| (c) 실제 무인용 | **5** | 5 / 0 | g47·g48·g49·g50·g52 |

**해석**
- **(b) 45건이 절대 다수** — 답변에 실재 문서명은 있으나 제N조 표기가 아예
  없다(표본 전수 `제N조 표기=0`). 즉 앵커 0 의 정체는 추출기 버그가 아니라
  **문서 단위 인용을 앵커 쌍 정의가 담지 못하는 구조**. C-class 76/101
  코퍼스에서 정상 동작의 결과다 → 처방은 **conf 서수화(UNVERIFIED 분리)
  + 문서수준 인용 형식 의무화**(wave 2)로 정확히 배분된다.
- **(c) 5건은 전부 negative 문항**(g47~g52) — 무응답 표명이라 인용 자체가
  없어야 정상. **정상 동작 확인**, 처방 불요.
- **(a) 2건은 재분류 필요 — 실제로는 “외부 법령 조항 인용”.** g27 답변의
  제N조 4건은 전부 사규 조항이 아니라 **산업안전보건법 제128조의2 ·
  산업안전보건기준에 관한 규칙 제79조**다. 내 독립 근사가 근접 문서명
  (「휴게시설 관리지침」)과 잘못 묶은 것이고, `citations.py` 가 쌍을 만들지
  않은 것은 오히려 보수적 정답에 가깝다.
  **⚠️ wave 2 신규 입력**: 합성 계약·앵커 사전은 **사규 조번호와 외부 법령
  조번호를 구분**해야 한다. 구분 없이 “투입 청크 실재 조번호만 허용”을
  적용하면 법령 인용이 위조로 오판되거나(과잉 차단), 반대로 법령 조번호가
  ok 로 세탁돼 HIGH 등급 원료가 될 수 있다. (참고: 골든 g27 의 기대 인용
  “제79조”도 법령 조항 표기이며, 원장에 이 표기가 들어간 것은 문서 본문이
  인용한 법조문이 text 재추출에 포함됐기 때문 — 골든 구축 시 “표기 수준
  원칙”과 일관.)

---

## ⓪-3 제3 프록시 확인

**결론: 제3 프록시 후보 없음 (강한 간접 증거) — 최종 확정은 감리자 원문 대조 필요.**

- 직접 확인 한계: 원문 폴더(`사내지침_26년 1월 개정/`)는 감리자 세션에만
  연결돼 있어 이쪽에서 파일 실재를 볼 수 없다. `nexus_documents.
  source_storage_path` 는 **101건 전부 비어 있음** → DB 만으로 원문 보유
  여부 판별 불가.
- 대신 확보한 결정적 간접 지표 — **적재 원본 파일명 명명 규칙**:
  active 101건 중 **99건이 `카테고리│담당팀│제목│Rag.docx` 표준 규칙**
  (전부 .docx). 규칙 이탈은 정확히 **2건**이고, 그 2건이 G2 프록시 2건과
  **완전 일치**한다:
  | 문서 | source_filename |
  |---|---|
  | (공통) 신세계그룹 CREDO | `(공통) 신세계그룹 CREDO.docx` |
  | (공통) 임직원 징계기준 | `임직원_징계기준_v5.docx` |
- 즉 “원문 미확보 = 별도 경로 적재 = 명명 규칙 이탈” 신호가 2건에서만
  관측된다. **제3 프록시 명단 추가 없음**으로 wave 3 를 진행하되, 감리자가
  폴더 파일명 101건과 위 목록을 대조하면 즉시 확정 가능 (요청 시 전체
  파일명 목록 제공).

---

## ⓪-4 공수 추정 (날짜 확정 아님 — 감리자 판단 입력용)

| 단계 | 작업 | 추정 |
|---|---|---|
| wave 1 | 단계 타이머 5종 + 하니스 기록 필드(청크 수·입력 토큰·라우터 히트) + 테스트 | **0.5일** |
| wave 2 | 앵커 사전 파생 · 합성 계약 개정(법령/사규 구분 포함) · verify 문서명 검증 · conf 서수 4등급 + 소비처 3곳 · 테스트 개정 | **1.5일** |
| wave 3 | doc_router 신설(라우팅 소스 설계 포함) · force_include 예약석 · 프록시 감점·쿼터 · 예산 파라미터 · 플래그 · 라우터 단위 테스트 | **2.0일** |
| 재검증 | 하니스 재러닝(90문항×2 계통, 깊이 증가 반영 **약 1.5~2시간 wall-clock**) + 위조 8건 재주입 + 홀드아웃 3문항 + 채점·결과 문서화 | **0.5일** |
| — | 합계 | **약 4.5일** (연속 작업 기준) |
| — | 리스크 버퍼(라우터 정확도 미달 시 재설계·정지 조건 대응) | **+1.0일** |

가장 큰 불확실성은 **wave 3 doc_router 의 라우팅 소스**다 — v1 은 문서
카탈로그 기반 LLM 라우팅을 쓰는데, v2 는 `nexus_documents` 의
`auto_keywords` / `auto_query_examples` / `title` 을 SELECT 로 활용 가능한
것으로 보인다(컬럼 실재 확인됨). 구어체 질의 정확도(≥90% 기준)가 여기서
갈리므로, 승인 시 wave 3 착수 전 라우팅 소스 소규모 검증을 먼저 돌리는 편이
안전하다 (지시서 범위 내 판단 요청 사항).

---

## 정지 — 승인 대기

지시서대로 **⓪ 완료 지점에서 정지**한다. wave 1~3 착수하지 않았고, 엔진·
설정·코퍼스 어느 것도 변경하지 않았다(diff = 본 문서 + STATUS/OBSERVATION
기록만). 감리자 판정 후 wave 개시 지시를 기다린다.

**승인 시 즉시 반영할 프로브發 설계 조정 2건 (승인 필요)**
1. wave 2 합성 계약·앵커 사전에 **외부 법령 조번호 구분 규칙** 추가 (⓪-1 a군).
2. wave 3 착수 전 **라우팅 소스 소규모 검증** 선행 (⓪-4 불확실성).
