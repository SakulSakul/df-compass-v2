"""프록시 문서 명단 — 원문 미확보 상태로 적재된 문서 (심의 2항, wave 2·3).

⓪-3 프로브(2026-08-01): 표준 명명 규칙 이탈 2건이 당초 명단.
원문 확보(정본 확인) 시 명단에서 제거(승격) — 비차단 병행 트랙.

승격 이력:
- "(공통) 임직원 징계기준" — 지시서 008(2026-08-16), 저장본 v5 정본 확인
  의결 + 청킹 무결성 검사 근거.
- "(공통) 신세계그룹 CREDO" — 지시서 009(2026-08-16 밤), G1~G4 심의 최종
  의결(사용자 반증 "모든 사규가 CREDO 기반 — 헌법" + 정본 확인 의결) +
  청킹 무결성 검사(조문 4종 원장 ok·장 1~5·중복/빈 0) 근거.

현행 명단 공집합 — 전 기제(리랭크 감점·쿼터·MEDIUM 상한)는 휴면 유지,
로직 제거 금지(감리자 즉답 08-16, 본문 동급 효력). 근거: ① 범위 최소
② 명단은 재사용 가능 안전 장치 — 원문 미확보 문서 신규 적재 시 1행 추가로
전 기제 재가동 ③ 안전 표면 인접 로직의 제거는 별도 심의 대상.
"""
from __future__ import annotations

PROXY_DOCS: frozenset[str] = frozenset()
