# DF COMPASS v2 — 구조 개선 통합 심의 안건서 (v2, 2026-07-30)

> 사용자 확정본 원문 등재 (2026-07-30 접수). 이 파일이 심의 안건의 정본이다.

## 심의 개요
- 형식: council 5인 (역산·문제분해·유추·순진한질문·의존성그래프) — 무조건 풀 심의
- 성격: 검색 보장 × 프록시 등급 × confidence × 지연 예산을 하나의 합성
  계약으로 묶는 설계 심의
- H 안건 재편: 기존 H 안건 2번(confidence·번호체계·합성계약 3측 묶음)은 본
  심의에 흡수. H 안건 1번(critical 커버리지 미발동 4/8)은 컷오버 후 유지.
- 결정 근거: 구조 진단 §1~5(커밋 6ac5978) + §6 지연 분해(커밋 d50de2c)

## 개시 조건 (입력 완비 체크리스트)
1. [ ] DIAGNOSIS_RETRIEVAL.md 원문 확보 → 감리 실물 대조 통과
2. [x] 진단 §6 지연 분해 결과 도착 (07-30, 커밋 d50de2c)
3. [ ] 07-30 사건 원문 질의 A' 재판정 결과 반영
4. [x] 비교 하니스 JSON (compare_v1v2_20260728T114752.json, 감리 적합)
5. [x] G2 전수 감사 판정 (B 0/208, C-class 76/101)

## 심의가 결정할 것 (8항)
1. **보장 계층 설계**: v1형 doc_router+force_include 이식 여부·형태,
   rerank_pool(현 15)·top-k(현 3) 예산 — B 5건·A' 2건(경계 16~18위) 실측 근거
2. **CREDO 도핑 처리**: 단일 문서 과대표성(프록시 인용 15/15 전부 CREDO,
   top3 도배) — 프록시 등급제/검색 감점/원문 확보 선행 여부. CREDO 제N조
   4건의 원장 등재가 ok→conf 1.0 세탁 원료가 되는 구조 차단
3. **confidence 산식 재설계**: 현재 사실상 이진(0.0/1.0). 근거 등급
   (원문/프록시/강등) 연동 — "프록시 전용 & conf 1.0" 15건 재발 방지
4. **번호 체계·합성 계약** (구 H 안건 2번): D 5건 — 인접 조 인용 2건(g23,
   g46) + 조번호 미표기 3건. 법인카드 지침([부칙]뿐인 원장) 유형의
   article_missing 메커니즘 포함
5. **앵커 0 정체 판정**: 무인용 답변 52/89(58%)이 앵커 추출 실패인지 실제
   무인용인지 — 심의 중 판별, 필요 시 선행 프로브
6. **지연 예산·계측**: §6 확정 — 지연·재현율은 동일 트레이드오프의 양면으로
   심의. 12청크급 추정 p50 22~32s(표본 2점, 구간으로만 인용) 대비 허용 지연
   상한 설정. v2 검색 내부(embed/RPC/rerank/neighbors) 계측 추가 여부 결정
   (현재 리랭커 성공 시 무로그)
7. **구현 순서·재검증 계획**: 심의 확정 후 일괄 구현 → 하니스 재러닝(동일
   프로토콜) → 독립 재계산 감리 → 통과 기준(재현율·위조·neg·지연) 사전 명시.
   재검증 지연 기준은 §6 반영해 "깊이 동등 조건" 명시
8. **컷오버 일정 영향**: 8/10 은 조건부 — 재검증 성적표 확보 시에만 컷오버
   심의 개시. 슬립 시 신규 일정 산정

## 제약 (상시 규칙 유효)
- 심의 확정 전 구현 착수 금지 (규율 H). 구현은 확정 설계의 일괄 반영만
- v1(nexus_ai) 수정 금지 · DB 쓰기 금지 · 키 채팅 금지 · 검증 없는 완료 선언 금지
- 감리 판정 = 커밋 실물 대조 + 결과 JSON 독립 재계산 + 브라우저 육안 확인

## 진단 확정 사실 (심의 입력 요지)
- 미스 계급: A 0 / A' 2 (rank 17·18, pool=15 경계 밖) / B 5 (풀 내 도달 후
  컷, top3 CREDO 도배) / C 0 / D 5 (조항 수준) → B+A' 7/12(58%) 보장 계층,
  D 5/12(42%) H 묶음
- 07-30 사건: A'급 라이브 재현 (간접·상황형 질의 의존, g40 동형) — 원문
  질의 재판정 대기
- conf 교차표(n=89): 이진 분포. 프록시 전용 & conf≥0.8 = 15건 전부 conf 1.0.
  앵커 0 = 52건 전부 conf 0.0
- 프록시 빈도: 15/89(16.9%) 전부 CREDO, 징계기준 0
- `verify degrade ok=0 am=3`: 실강등 (07-29 법인카드 질의, 원장=[부칙]뿐 →
  제N조 표기 즉시 am). 대상 3쌍 재특정 불가(비결정 출력)
- §6 지연 분해: 컷오버 성적표 지연 항목 "설계 부산물"로 최종 정정. synth_ms
  전건 기록·검색 합계 파생 가능, 내부 분해 계측 부재. synth×답변길이
  r=+0.774(디코드 지배), 12청크 직접 프로브(2문항) Δneighbors +2.3~2.6s·
  Δsynth +4.6~13.8s → 추정 p50 22~32s, v1 실측 23.3s 구간 하단 정합
