# DF COMPASS v2 — R2 보정 심의 결과 (2026-08-05)

> council 5인 + 리뷰어 2, 결측 0. 리뷰어가 원장 오프라인 재구성으로
> check_citations·채점기 재실행 — 핵심 주장 기계 확증. 원문 등재 (정본).
> **상태: 사용자 판정 대기 — 수용 시 R3 구현 지시서 발행. 그 전까지 구현 금지.**

## 확정 판정
- 재검증 FAIL 4문항의 실체:
  - **g02 = 합성 절단** (finish_reason 미검사, max_output_tokens 4096;
    변형 g31v1 동종 — 재발성 계열)
  - **g27·g28 = 골든 기대쌍이 외부 법령 조번호의 사규 오귀속 — 확정**
    (산안법 제128조의2·산안기준규칙 제79조 / 대규모유통업법 제11조)
  - **g23 = 동종 의심** (개보법 제25조·25조의2) **미확정 — 프로브 분기**
- 구 시스템 "hit" = 오염 원장(registry 가 법령 필터 없이 전문 스캔) × 인용 창
  아티팩트의 **가짜 양성**. 구 기준 충족의 유일 경로 = D1 수행 (기계 재현
  입증). g27 에서 살아있는 D1 관통 실측: (지침, 제128조의2) verify ok·HIGH.

## 채택 설계
1. (b) 계약 완화 **기각(만장일치)**. (a)는 이번 4건 no-op — g23 분기용 +
   미래 D2 백로그.
2. **원장 정화**(A1 필터, 보수 규칙: 비법령 출현 1회면 등재 유지) +
   **is_law_ref 개재어 보강** — 이번 라운드 동시 적용(인터록). 선행: 정화
   전후 오프라인 전수 diff 프로브. 손실 발견 시 폴백(태거만 이번).
3. **골든 라벨 정정**: g27·g28 확정, g23 프로브 후 분기. 정정 라벨 3조건형 =
   법령명+조번호 정확 표기 / 사규 doc 인용 / 오귀속 ok쌍 부재. **매처 코드
   수정 명시 의결.** 문서수준 후퇴 옵션 기각.
4. **g02 무결성 게이트**: 계약 미충족 시 재합성 1회→강등, finish_reason
   계측, Claude 폴백. 변형 트랙 포함.
5. **감리 장치 6종**: g23 프로브 선행 / 정화·태거 동시 커밋 / 재러닝 직전
   원장 덤프 재생성 / 청크 원문 발췌+법령 원문 대조 첨부(자기참조 절단) /
   매처 수정 명시 / 신·구 이중 성적표 + ③ 기준선 스냅숏 2벌·분모 명세.
6. **절차**: 오프라인 드라이런으로 산술 확정(기대 ② 2/5→4/5, ③ 2→1) →
   R1 라우터 보정과 합쳐 라이브 재러닝 1회 → 독립 감리. 변형·홀드아웃 골든
   복제 확인.

## 소수의견 (기각)
정화 전면 차기 이월(분해 — D1 관통 방치, 리뷰어 2인 일치 기각) / 태거만
이번+문서수준 후퇴 옵션(의존 — 폴백으로만 보존) / g23 확정 서술(역산·분해 —
분기로 대체) / 채점기 동결 프레임(신참 — 매처 수정 불가피) / 태거 없는 정화
단독(유추 — 정답 오강등 역회귀).

## Kill Criteria
- diff 에서 통과쌍 앵커 소실 → 정화 폴백
- g23 자체 조문 실재 → 재귀속 철회·(a) 전환
- 정정 라벨을 오귀속 답변이 통과 → 엄격화 폐기·재심의
- 드라이런 ② <4 → 재심의
- 위조 비강등 ≥1 → 롤백·동결
- 게이트 후 절단 재발 → 출력 예산 별도 심의
- 정정 전 기준 신규 FAIL → 발효 보류

## 절차 요약 (수용 시 R3)
⓪ 프로브 3종 → ① 정화+태거·g02 게이트 → ② 골든·매처 정정 → ③ 드라이런
→ 재러닝 1회 → 감리.
