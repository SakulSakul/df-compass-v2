# 지시서 001 완료 보고 — 중단 복구 간소안

| 항목 | 값 |
|---|---|
| 지시서 | 001 (중단 복구 간소안) |
| 상태 | **완료** |
| 커밋 | f1b3f7b (최초 구현) + 4bde523 (판정식 정합화) |
| 테스트 | 110 passed, 2 skipped (전체 스위트 — 무회귀) |

## 작업 이행

1. **inflight 기록**: 처리 시작 시 `session_state["_inflight"] = {"q": 질문, "ts": time.time()}`,
   엔트리 append 직후(모든 분기 — 엔진/코치/한도/오류) 해제. 최초 구현은 문자열
   마커였고 4bde523 에서 지시서 형식 {질문,ts} 로 정합화.
2. **감지·배너**: 렌더 상단에서 잔존 마커 + 마지막 메시지가 동일 질문의 user 턴
   (답변 부재)일 때 "⚠️ 직전 답변 생성이 중단되었습니다" 배너 + [🔁 이 질문 다시
   실행] 버튼 (영속 엔트리 — replay 생존).
3. **재실행**: 마커 소거 선행(감지 시점 pop + 버튼 클릭 시 rerun_done 마킹) 후
   pending_q 적재 — 기존 단일 제출 경로·동시 제출 잠금 그대로 통과. 1회 한정
   멱등(재클릭 불가, "다시 실행됨" 캡션 전환).
4. **TTL 없음** — 소거는 재실행 또는 정상 완료 시만. 부가 소거 1건 명기:
   ⏹ 의도된 중지 시에도 소거(중지 안내와 중단 배너의 중복 방지 — 목적 정합).
5. **카운터**: 발동 시 `[ui:abort] q_len=N` stderr 1줄 (30일 관측 데이터, ADR-8 준수).
6. 자동재시도·원인 분류 — 미구현 (심의 기각 준수).

## 수용 기준 자체 검증

| 기준 | 결과 | 근거 |
|---|---|---|
| (a) 강제 종료 재현 시 배너+버튼 | PASS | 라이브 2회 실측(처리 중 selectbox 값 변경으로 run 킬 → 배너 표출. 문자열 마커 1회 + dict 마커 1회) — d001_banner.png |
| (b) 1클릭 동일 질문 재실행 완주 | PASS | 라이브 실측(재실행 → 답변 완주 True) |
| (c) 정상 완료 후 배너 미출현 | PASS | 어서션 test_no_false_banner_after_completion + 라이브 정상 런들에서 미출현 |
| (d) 재실행 이중 발화 0 | PASS | 어서션(멱등 — 클릭 후 버튼 소멸) + 라이브 멱등 True |
| (e) 기존 테스트 무회귀 | PASS | 110 passed (지시서 기준 104 전건 포함) |

어서션 4건: test_interrupt_recovery_banner(마커 라이프사이클+배너+멱등) /
test_no_false_banner_after_completion(오탐) / test_stop_suppresses_interrupt_banner
(중복 배너) / 기존 스위트 무회귀.

## 특기 사항 (감리·판정 참고)

- 배너 표출 시점: run 킬은 Streamlit 실행 모델상 다음 st 호출에서 발생하므로
  합성 블로킹 구간에 걸리면 배너가 수 초 지연될 수 있다 (v1 동일).
- 서버 재시작 시나리오: session_state 세션이 통째로 소실되면 질문 말풍선
  자체가 없으므로 배너 대상 아님 — 마커는 질문 말풍선과 동일 수명 매체로
  생존성 등가 (코드 주석 명기).

불변 준수: 엔진 0줄 / v1 무수정 / DB 쓰기 0.
