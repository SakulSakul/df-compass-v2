# 지시서 002 완료 보고 — 파리티 원장 정본화 + IMPLEMENT 3건

| 항목 | 값 |
|---|---|
| 지시서 | 002 (파리티 원장 정본화 + IMPLEMENT 3건) |
| 상태 | **완료** (2026-08-16 밤 — CSV 정본 수신·등재, 블로커 해소) |
| 커밋 | 4bde523 |
| 테스트 | 110 passed, 2 skipped (skip 2건 = 원장 게이트 — CSV 미수신 사유 명시) |

## A. 원장 정본화 — 블로커

지시서 근거에 "파리티 원장 CSV(감리자 생성·릴레이 첨부)"로 명시됐으나 릴레이
메시지에 CSV 본문이 포함되지 않았다. 이행분:
- 게이트 테스트 등재(tests/test_parity_ledger.py): 전 행 disposition 존재 어서션
  + IMPLEMENT 잔존 0 어서션 + 허용 처분 어휘 검증 + '#' 주석 행 파싱.
  IMPLEMENT→PORTED 갱신 규칙은 테스트 docstring 에 명기(원장 수신 시 CSV 헤더
  주석에도 동일 문구 등재 예정).
- CSV 부재 상태에서는 **skip 으로 표시**(통과 위장 금지 — skip 사유가 출력에 남음).
- **요청**: 원장 CSV 본문(또는 파일) 릴레이 시 docs/parity-ledger.csv 등재 +
  IMPLEMENT 3건(본 지시서 B·C·D)을 PORTED 로 갱신하는 마감 커밋 1건으로 종결.

## B. SRMS 링크 — 완료

렌더 직전 결정론 치환 `_linkify_srms`: 비링크 독립 토큰 SRMS →
`[SRMS](https://rms.shinsegae.com/)`. 이중 치환 금지(직전 문자 `[`·영숫자·`/`·`.`
또는 직후 `]`·영숫자·`(`·`/` 면 제외 — 기링크·URL 내 출현·영문 합성어 차단, 한글
조사 인접은 독립 토큰). 호출부에서만 적용 — 저장 원문 불변이라 verify·DOCK·판정
카드 등 문면 파서는 원문을 소비한다(심의 보강 ⑤ 정합).

## C. 등급 라벨·메타 행 — 완료 (해석 1건 명기)

- HIGH = "🟢 높은 신뢰도 — 사규 원문 대조 확인" / MEDIUM = "🟡 보통 신뢰도 —
  참고 문서 기반, 원문 대조 권장" (지시서 문구 그대로).
- UNVERIFIED 중립·경고 상단 라벨, LOW 강등 칩 — 현행 유지. 산식·action 무접촉.
- 하단 메타 행 `[카테고리 · 등급 · ⏱️응답 N초 · 🤖모델]` 조립, 상단 카테고리
  뱃지 제거(상하단 중복 0).
- **해석 명기**: HIGH/MEDIUM 등급 표시는 메타 행으로 통합하고 상단 라벨을
  제거했다 — 상단·하단 양쪽에 등급을 두면 지시서가 카테고리에 적용한 것과 같은
  상하단 중복이 등급에서 발생하기 때문. UNVERIFIED 계열은 "현행 유지" 명문에
  따라 상단에 남긴다. 이 해석이 목적과 어긋나면 지시서 결함 처리 절차대로 판정
  요청.

## D. 히어로 플레이스홀더 — 완료

"사규·윤리 관련 무엇이든 물어보세요…" (v1 ui/home.py:99 원문, 바이트 일치
어서션).

## 수용 기준 자체 검증

| 기준 | 결과 | 근거 |
|---|---|---|
| (a) 게이트 테스트 전건 처분 어서션·통과 | **BLOCKED** | 테스트 등재 완료, CSV 미수신 → skip(사유 명시). 수신 즉시 활성 |
| (b) SRMS 포함 답변에 클릭 가능 링크·이중 치환 0 | PASS | 어서션(각 1회·이중 치환 0·원문 불변) + 라이브: 사건사고 질문 답변에서 `a[href="https://rms.shinsegae.com/"]` 5개 실측 — d002_answer.png |
| (c) HIGH "높은 신뢰도"·UNVERIFIED 중립 유지·강등 칩 무변경 | PASS | 어서션 test_grade_meta_row (4등급 전 케이스) |
| (d) 메타 행 4요소·상단 중복 0 | PASS | 어서션(HIGH 엔트리 4요소 공존 + 카테고리 엔트리당 1회) |
| (e) placeholder v1 원문 바이트 일치 | PASS | 어서션 + 라이브 실측 True |
| (f) 기존 테스트 무회귀 | PASS | 110 passed (구 어휘 어서션 2건은 신 어휘로 개정 — 002-C 의도 변경분) |

불변 준수: 엔진 0줄 / v1 무수정 / DB 쓰기 0 / 포맷 4규칙(W1) 미착수.


## 마감 추기 (2026-08-16 밤 — 지시서 002-A 마감)

- 첨부 정본 CSV 를 docs/parity-ledger.csv 로 무편집 등재 (UTF-8·개행 보존 —
  cp 바이트 복사).
- 게이트 활성: **전체 113 passed, 스킵 0** (감리자 동일 CSV 실측값과 일치).
- 재계산 일치: 행수 1,082 / 처분 집계 PORTED 63·SEMANTIC 50·EXCLUDED 660·
  PENDING 309·IMPLEMENT 0(부재) / 전 필드 단일행(개행 유입 0).
- 005 참고 사규 카드 행: 사전 판정대로 원장에 별도 행 없음 = 정상, 조치 불요
  (v1 리터럴은 한글 정규화 5자 미만으로 추출 규칙 밖, 카드 구조는 시각 51클래스
  축 기해소 — 커밋 17df728·육안 PASS).
