# 지시서 004 완료 보고 — 합성 모델 기본값 3.7 전환

| 항목 | 값 |
|---|---|
| 지시서 | 004 (합성 기본값 gemini-3.7-flash 전환) |
| 상태 | **완료** |
| 커밋 | 7b04870 (롤백 = 이 커밋 revert) |
| 테스트 | 110 passed, 2 skipped (무회귀 — skip 2 = 원장 CSV 대기 기존분) |

## 작업 이행

- compass_engine/synthesis.py: 기본값 두 곳(:97 생성 호출·:172 provider 라벨)을
  모듈 상수 `_DEFAULT_CHAT_MODEL = "gemini-3.7-flash"` 단일 참조로 통일.
  모듈 docstring 의 기본값 서술 동기화. 리랭크(NEXUS_RERANK_MODEL 기본
  gemini-3.5-flash-lite) 무변경. 그 외 엔진 무접촉.
- 진행 중인 지시서 003 A/B 러닝과의 간섭 없음 — 러너는 러닝별로
  NEXUS_CHAT_MODEL 을 명시 주입(기실행 프로세스는 구 코드 로드 상태이기도 함).

## 수용 기준 자체 검증

| 기준 | 결과 | 근거 |
|---|---|---|
| (a) 메타 행 🤖 gemini-3.7-flash | PASS(로컬) | 운영 경로 직배선 스모크에서 provider="gemini:gemini-3.7-flash" 실측 — 메타 행은 provider 를 그대로 표기. 배포본 표기는 배포 반영 후 감리·사용자 육안 |
| (b) 스모크 1문항 정상 완주 | PASS | 선물 질문 완주 — HIGH·pass·계약 통과·12.1s (참고: 동일 질문 3.6 은 18~19s) |
| (c) 기존 테스트 무회귀 | PASS | 110 passed 2 skipped |

## 검증 방법 결함 1건 자기 신고 (감리 참고)

1차 스모크를 A/B 하니스(Harness)로 수행했다가 provider 가 3.6 으로 나왔다.
근본원인: eval/v1_sidecar.py:109 가 v1 사이드카 init 시
`os.environ.setdefault("NEXUS_CHAT_MODEL", "gemini-3.6-flash")` 로 하니스
프로세스 env 를 고정하기 때문 — **운영 배포 경로(app.py)는 v1_sidecar 를
import 하지 않으므로 무관**하며, 스모크를 운영 경로 직배선으로 재수행해
PASS 를 확인했다. setdefault(기설정 존중)라 003 A/B 러닝은 오염 없음
(러너가 import 전에 명시 주입). 교훈: 운영 기본값 검증은 하니스 경유 금지.
