# 지시서 005 완료 보고 — 참고 사규 카드 디자인 v1 정합

| 항목 | 값 |
|---|---|
| 지시서 | 005 (참고 사규 카드 v1 정합) |
| 상태 | **완료** |
| 커밋 | (본 커밋) |
| 테스트 | 111 passed, 2 skipped (무회귀 — skip 2 = 원장 CSV 대기 기존분) |

## 작업 이행

- v1 ui/render.py:299-328 구조 이식: 청크당 nx-doc-card(테두리·상단 4px 블랙)
  + nx-doc-badge(검정 캡슐 10px) + nx-doc-title(13px 볼드) + nx-doc-cite(조번호
  우측 정렬) + nx-doc-text(스니펫 480자). CSS 는 ui/styles.py nx-doc-* 원문.
- **배지 분기**: v2 RetrievedChunk 는 RPC 의 doc_kind 를 실어오지 않음(엔진
  무접촉이라 확장 불가 확인) → 지시서 분기대로 문서명 결정론 매핑 —
  "(공통) 임직원 징계기준"→징계기준 / "(카테고리) …" 명명 규약→사규 /
  판별 불가→DOC. 추측성 배지 없음.
- **조번호**: 청크 article_no(파서 검증 canonical — v1_retriever 가 RPC 값 을
  parse_article_ref 로 정규화)만 표기, 없으면 미표기 — 환각 조번호 0.
- **escape**: 배지·제목·조번호·스니펫 DB 출처 값 전부 html.escape (v1 stored
  XSS 교훈 승계). 렌더 API 는 v2 관례(st.markdown unsafe_allow_html)로 통일
  (v1 은 st.html — 구조·CSS 동일, 산출 DOM 동일).
- 구 히스토리 {crumb,snippet} 형식 폴백 렌더 지원 (세션 내 기존 엔트리 보호).
- 파리티 매트릭스에 시각 구조 사각 보완 기록 1행 갱신 — 원장 CSV 수신 시
  동일 행을 CSV 에도 등재 예정 (002-A 마감 커밋에 포함).

## 수용 기준 자체 검증

| 기준 | 결과 | 근거 |
|---|---|---|
| (a) 청크당 카드+배지+제목+스니펫 | PASS | 어서션(카드 3/3 렌더) + 라이브 사건사고 질문에서 DOM 실측 카드 5·배지 5 |
| (b) 징계기준 배지 | PASS | 어서션('nx-doc-badge">징계기준') — 라이브는 프록시 쿼터로 징계기준 문서 비포함 회차라 AppTest 고정 |
| (c) 조번호 실재값만·환각 0 | PASS | 어서션(cite 스팬 = 조번호 보유 카드 1개만) + 라이브(문서수준 청크 5건 → cite 0 표기) |
| (d) DB 값 전부 escape | PASS | 어서션(&lt;script&gt; 무력화 확인) |
| (e) 기존 테스트 무회귀 | PASS | 111 passed 2 skipped |

부수 실측: 라이브 메타 행에 🤖 gemini-3.7-flash 표기 — 004 수용 기준 (a)의
로컬 실측 재확인.

불변 준수: 엔진 0줄(렌더 전용) / v1 무수정 / DB 쓰기 0.
