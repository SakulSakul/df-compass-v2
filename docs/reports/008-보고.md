# 지시서 008 완료 보고 — 임직원 징계기준 원문 대조 → 프록시 승격

| 항목 | 값 |
|---|---|
| 지시서 | 008 (통합 최종본 — Kill Criteria 발동 승격) |
| 상태 | **완료** (승격 커밋 포함) |
| 테스트 | 111 passed, 2 skipped (무회귀 — skip 2 = 002-A CSV 대기) |

## A0 — 원문 소재 확인 (SELECT 실측)

row a771e64a: 원문 텍스트 컬럼 **부재**(auto_summary 등 파생 필드뿐),
`source_storage_path='None'`, storage 버킷 **0개**, v1 repo 내 DOCX **부재**.
→ 독립 원문이 시스템 어디에도 없음 — 추출 방법은 "청크 전수 SELECT"가 유일.

## A — 대조 해석 규칙 발동: 청킹 무결성 검사로 수행

독립 원문 부재로 "정본↔적재 일치" 검사는 성립 불가 → 지시서 해석 규칙대로
**청킹 무결성 검사**(내부 일관성)로 성격 전환. 정본성 근거 = 사용자(운영자)가
이 저장본을 현행 정본으로 확인한 의결(2026-08-16), 정본 버전 = v5.

결과 (eval/results/penalty_integrity_20260816T191115.json — 청크 13건 원시 동봉):
- 징계 수위 명단 **8종 전부 출현** (경고장 12·서면경고 37·견책 84·감급 66·
  감봉 77·정직 44·강격 36·징계해직 40회)
- 비위행위 항목 번호 **1~40 연속, 결번 0**
- 양정 조건 실재 (가중 17·감경 4·관리책임 87·인사위원회 1·자진 신고 1)
- 표제 존재·중복 청크 0·빈 청크 0·총 14,772자
- 참고 기록: 본문 내 제정일 표기 "(2019.6.1)" vs effective_date 2026-05-07
  (개정 이력 표기 — 무결성 신호 아님)

## B — 분기 판정: 실질 무결 → C 진행

## C — 승격 커밋

- proxies.py PROXY_DOCS 에서 "(공통) 임직원 징계기준" 1건만 제거(CREDO 잔존)
  + docstring 에 승격 이력·근거(v5·사용자 의결 08-16) 기록.
- 형제 전수 grep: 소비 지점 = verify.py:72-73(proxy_cited → MEDIUM 상한),
  v1_retriever.py:83-85(_is_proxy — 감점·쿼터), recert_score(_r3).py(채점기 —
  명단 import 라 자동 추종). 하드코딩 문서명 잔존 = tests/eval 픽스처의 원장
  등재용 제목 사용뿐(프록시 가정 아님 — 무접촉 정상).

## D — 검증

① 전체 테스트 111 passed 무회귀. **프록시 가정 테스트 갱신 불요** —
   test_verify(프록시 상한)·test_guarantee_layer(감점·쿼터) 전부 CREDO 기준.
② g14 전후: 전(A/B/base 러닝) UNVERIFIED·ok 0/0 → 후 UNVERIFIED·ok 0/0
   — **등급 변화 없음이 정상**: 징계기준은 C-class(조번호 앵커 없음)라
   MEDIUM 상한 해제의 수혜 조건(앵커 ok≥1)에 해당 없음. 문서수준 인용·
   ⚖️ 섹션은 실재(원시 JSON promotion_check_g14_live2.json).
③ 라이브 2문항(운영 직배선): live1 "거래처에서 선물을 받아도 되나요?" →
   **HIGH**·징계기준 ctx 진입·인용·⚖️ 섹션 (승격 전엔 프록시 인용 → MEDIUM
   상한 대상 — 상한 해제 실증) / live2 "법인카드를 개인 용도로…" →
   UNVERIFIED·징계기준 ctx 진입·인용·⚖️ 섹션. 두 문항 모두 승격으로
   감점·쿼터가 사라져 징계기준 청크가 자유 진입.
   암묵 의도 질문의 미노출 문제는 검색 미진입 논점(007 P0)이며 승격 실패와
   무관 — 007 보고에서 구분 측정.

수용 기준: (a) 무결성 JSON 실물+판정 근거 ✅ (b) diff = proxies.py 1건 제거
+eval 신규 프로브(운영 코드 외) ✅ (c) 테스트 출력 실물 ✅ (d) g14 전후·라이브
2문항 원시 기록 ✅. 불변: v1 무수정·DB 쓰기 0(SELECT만)·안전 표면 무변경.
