# 지시서 009 완료 보고 — CREDO 승격 + G3′ 오염 관측 (③b 계열 종결)

| 항목 | 값 |
|---|---|
| 지시서 | 009 통합 최종본 (원 009 + 부기 + 감리자 즉답) |
| 상태 | **승격 완료 / G3′ 관측 완료 — 판정 미통과(랭킹 보정 별도 안건 상정 필요)** |
| 테스트 | **114 passed, skip 0** (113 + 휴면 실증 테스트 1 신설) |

## A0 (소재)

row `aedc97fe-8ccd-4973-b656-9c1b14faaf74` — title "(공통) 신세계그룹 CREDO",
doc_kind rule, 원본 파일 "(공통) 신세계그룹 CREDO.docx", source_storage_path
= None·storage 버킷 0 (008 과 동일 환경) → 독립 원문 부재, 유일 저장본 =
nexus_chunks **22청크**. 추출 방법 = 청크 전수 SELECT.

## A (무결성 검사) — eval/results/credo_integrity_20260816T205141.json (청크 전문 동봉)

- 조문형 확인: canonical 조번호 {제1조~제4조} × 장 구조 제1~5장 실재
  (CREDO 는 장별 조번호 반복 구조)
- **원장 앵커 등재 전수 ok** (제1~4조 모두 ledger.verify == "ok") — 승격 즉시
  앵커가 원장 대조 검증 대상이 되는 전제 확인
- 핵심 조항 실재: 제4조 금품/향응/편의 수수 금지·제2조 사원의 기본책무·
  지인거래 신고(SHRS CSR경영란)
- 표제 청크 실재·중복 0·빈 청크 0·총 16,086자
- **B 분기: 무결 → C 진행.** 정본성 = 사용자 정본 확인 의결(2026-08-16).

## C (승격 커밋)

- PROXY_DOCS → **`frozenset()` 공집합**. 처분 = 감리자 즉답(본문 동급 효력):
  전 기제(리랭크 감점·쿼터·MEDIUM 상한) **휴면 유지·로직 제거 금지**. 근거
  3항(범위 최소 / 재사용 가능 안전 장치 — 신규 미확보 문서 1행 추가로 재가동 /
  안전 표면 인접 로직 제거는 별도 심의) docstring·본 보고에 승계.
- 형제 점검(전 소비 지점 공집합 안전): `v1_retriever._is_proxy`(호출 시
  import — `any()` over empty=False), `verify.proxy_cited`(:72-73 — 동일),
  채점기 recert_score(_r3)(명단 import 추종). 하드코딩 잔존 없음.
- **프록시 가정 테스트 갱신 내역**: test_verify 2건 — 합성 명단 monkeypatch
  (verify 는 모듈 상단 import 라 `compass_engine.verify.PROXY_DOCS` 패치)로
  기제 검증 유지 / test_guarantee_layer 감점·쿼터 1건 — `compass_engine.
  proxies.PROXY_DOCS` 패치(메서드 내 import 라 유효) / **신설 1건**
  test_proxy_list_empty_dormant — 운영 공집합에서 CREDO 앵커 인용이 상한
  없이 HIGH (휴면 실증).

## D (검증) — eval/results/credo_promotion_live.json

- ① 전체 테스트 114 passed·skip 0.
- ② 라이브 "법인카드를 개인 용도로…": CREDO **ctx 미진입**(라우터는 법인카드
  지침 지목 — 정상 라우팅). 지시 구분대로 명기: **검색 진입 문제이지 승격
  실패 아님** (징계기준은 진입·인용, UNVERIFIED).
- ③ g13 "협력사 직원 접대 한도": **HIGH** — CREDO ctx 진입 + 인용 + **조번호
  인용**(credo_art_cited=true) + 앵커 ok 3/3. 승격 전(A런) g13 은 HIGH 였으나
  CREDO 인용 시 MEDIUM 상한 대상이었음 — 이번엔 CREDO 조문 인용이 원장
  대조를 통과하며 HIGH 유지 = **승격 효과 완전 실증**. 라우터는 이번 라이브에서
  CSR경영방침을 confident 지목(CREDO 는 검색 경로 진입) — 라우터 산출의
  라이브 변동으로 원시 기록.

## G3′ (오염 관측 — 발효 전 1회 결박) — eval/results/g3prime_20260816T205456.json

- 러너 요건(부기) 이행: **전용 러너 eval/g3prime_probe.py** — 문항별 최종
  context_docs 직접 기록 + proxy_pool_probe 확장(RRF 순위·리랭크 후 순위 병기)
  → ab37 선보강 불요 (러너 선택 명기).
- 결과: 7문항 중 6문항 top_k 진입 0 / **g47("오늘 점심 메뉴 추천해줘") 1건
  진입** — 임직원 징계기준이 RRF 14위 → **리랭크 1위** → 최종 컨텍스트 포함.
- **판정(사전 고정 규칙): 미통과 — 랭킹 보정 별도 안건 상정, 발효 보류
  사유로 보고.** 원시 참고: g47 은 무관 질문이라 답변 계층에선 무응답 표명
  대상(기존 러닝 negative 마커 통과 이력)이나, 컨텍스트 오염 자체가 관측
  기준이므로 규칙대로 기록. CREDO 는 7문항 전건 top_k 미진입(g10 리랭크
  15위가 최고 — top_k 밖).
- **명문 개정 1줄**: G3′의 "발효 전 1회 결박"은 "게이트=발효 비선결" 원칙의
  의장 개정 채택분이다.

## 수용 기준 자체 검증

(a) A0·무결성 JSON 실물 ✅ (b) 승격 diff = proxies.py + 테스트 갱신 외 운영
코드 0 ✅ (c) 테스트 출력 실물 114 passed ✅ (d) 라이브 법인카드·g13 전후
기록 ✅ (e) G3′ JSON — 7문항 × {최종 context_docs·승격 문서 리랭크 후 순위}
✅ (전용 러너 — ab37 diff 해당 없음).

불변 준수: v1 무수정 / DB 쓰기 0(SELECT·RPC 읽기만) / 안전 표면 3불변 무변경
/ secrets 값 출력 0.
