# 지시서 010 — P1 회부 보고 (N 확정 판정 재료, 정지 상태)

| 항목 | 값 |
|---|---|
| 단계 | P1 완료 — **사전 고정 규칙에 따라 정지·회부** (P2 미착수) |
| 실측 | eval/results/rank_trace_20260817T121844.json (52/52 완주·에러 0, 무편집) |
| 테스트 | 115 passed (계장 무영향 + 계장 구조 테스트 1 신설) |

## 이행 내역

- retrieve() 내부 3층 계장(v1_retriever.py — RPC 인덱스 캡처·주입 이전 시점·
  score 역산 금지·관측 전용 추가 키 `rank_trace`, 반환 청크 무영향).
- 골든 52 전수 실측 — **운영 동일 경로**(운영 RPC·쿼리 변환·intake 마스킹
  포함, 프로브식 별도 RPC 미사용).

## N 판정 재료 (독립 재계산용 원시는 JSON — 아래는 구현자 집계)

**(i) 관련 질문 rescue 분포 — 예상 반증, 대량 실재**

- 관련 질문 45문항 중 **36문항(80%)**에서 RRF>10 청크가 최종 컨텍스트 진입.
  rescue 청크 총 66개, 원 RRF 범위 11~25 (풀 전역).
- 그중 **승격 문서(징계기준·CREDO) rescue 14건** — 핵심 편익 문항 포함:
  g14(징계 수위) RRF 12→rerank 1, g13 징계기준 17→4, g03 CREDO 18→3,
  g41 CREDO 23→2·25→5, g08 징계기준 16→3.
- **함의**: "리랭커 급상승은 예외적 동작"이라는 창 설계 전제가 운영 계기에서
  반증됨 — 급상승 구제(rescue)는 v2 리랭크 파이프라인의 **일상 동작**이며,
  종전 판정 재료(편익 보존 N≥4 등)는 편향 계기(원문 query_text) 산출이었음이
  확인됨. 운영 계기에서는 승격 문서 편익이 RRF 11~25위 → 리랭크 1~5위
  구제로 실현된다. **N=10 창은 관련 질문 36문항의 최종 컨텍스트 구성을
  변경**(회귀 면적 대량)하며, N을 25(풀 전체)까지 올려야 rescue 0 — 즉 창이
  무의미해진다.

**(ii) 무관 그룹 승격 문서 운영 계기 순위**

- 7문항 중 **g47 1건만** 징계기준 진입 — 운영 계기 RRF **15위** → rerank 2위
  (프로브 편향 계기의 14위와 근사 — 오염 현상 자체는 계기 무관 재현).
  CREDO 는 전건 미진입.

## 사전 고정 규칙 적용

지시서 P1: "rescue 발견 시 전역 N 인상 금지 — 사례 목록과 함께 정지·보고
(예약석 확장 재상정은 감리자 안건)" / 감리자 판정 절차 ②: "rescue 존재 시
N 인상 없이 예약석 확장 안건으로 분기".

→ **rescue 존재(36문항·승격 문서 14건) — 정지하고 회부한다.** 구현자 판단
없이 원시 재료만 부기: 오염(무관 1건, RRF 15)과 편익(관련 36문항 rescue)이
같은 메커니즘(리랭커 급상승)의 양면이라, 순위 창 단독으로는 분리 불가능한
구조가 실측됐다 — N 후보 어느 값도 "차단 상한(15) < N < 편익 하한(11~25
분포)"을 만족하지 못한다 (차단하려면 N≤14, 편익 보존은 N≥25).

정지 — 감리자 N 확정(또는 설계 재상정) 신호 대기.
