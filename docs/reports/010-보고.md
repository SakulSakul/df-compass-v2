# 지시서 010/010-r1 — 완료 보고 (P2′ 라우터 조건부 리랭크)

| 항목 | 값 |
|---|---|
| 결론 | **게이트 (a)(b)(c) 전부 통과 — P2′ 발효 보류 해제** |
| 커밋 | (본 커밋) — P1 계장은 b62fb2d |
| 테스트 | **119 passed** (기준 115 + P2′ 신설 4) |
| 재실측 | eval/results/rank_trace_20260817T130946.json (52/52 완주·에러 0) |
| 판정 원시 | eval/results/p2prime_gateb_verdict_20260817.json / neg_reject_*.json |

## 1. 경위 — P1 반증과 자격 창 폐기

- P1 계장 실측(b62fb2d, rank_trace_20260817T121844.json): 관련 45문항 중
  36문항(80%)에 RRF>10 rescue(66청크, RRF 11~25) — "리랭커 급상승은 예외"
  라는 창 설계 전제가 운영 계기에서 반증됨.
- 오염(무관 g47, RRF 15)과 편익(관련 rescue)이 **같은 리랭크 급상승
  메커니즘**이라 순위 창으로 분리 불능 — 차단 상한 N≤14 vs 편익 하한 N≥25
  교차 불능. 사전 고정 규칙대로 정지·회부(010-P1-회부.md).
- 감리자 의결(010-r1): **자격 창 폐기**, rescue 32문서 분산으로 예약석 확장도
  기각 → **P2′ 라우터 조건부 리랭크** 채택.

## 2. P2′ 구현 (v1_retriever.py — 리랭크 블록 1개소)

- 라우터 **무지목**(확신 지목 없음, `routed_titles` 공집합) 시 리랭크 호출
  스킵 → RRF 순서 그대로 `top_k` 컷. **지목 시 현행 리랭크 유지.**
- 라우터 미구성(`router=None`) 시 스킵 없음 — 기존 동작 보존.
- 스킵 여부를 `rank_trace["rerank_skipped"]` 로 계장 기록.
- 하류 단계(프록시 감점·예약석·쿼터·cap·이웃) 무변경 — diff 는
  조건 1줄 + 근거 주석 + trace 기록 1줄.

## 3. 게이트 (a) — 단위테스트 ✅

`tests/test_guarantee_layer.py` 신설 4건 (역순 fake reranker 로 스킵/수행 구분):

| 테스트 | 고정 불변식 |
|---|---|
| p2prime_no_designation_skips_rerank | 무지목 → 스킵, RRF 순서 유지 |
| p2prime_designation_keeps_rerank | 확신 지목 → 리랭크 수행 |
| p2prime_no_router_baseline_rerank_unchanged | 라우터 미구성 → 스킵 없음 |
| p2prime_skip_downstream_unchanged | 스킵 시 감점·쿼터·cap 불변 |

전체 스위트 **119 passed** (회귀 0).

## 4. 게이트 (b) — 골든 52 전수 재실측 ✅ (P1 러너 동일 스키마 재사용)

판정 스크립트 `eval/p2prime_gateb_judge.py` (스킵 판별: `router_hit is None`
⇔ 무지목 — v1_retriever 구조상 동치):

| 항목 | 기준 | 실측 | 판정 |
|---|---|---|---|
| b1 무관 무지목 오염 | 승격 문서 top_k 진입 0 | 스킵 6건(g47·48·49·50·51·52) 진입 **0** — **g47 오염 소멸** | ✅ |
| b2 관련 rescue 보존 | 36문항·66청크 ±10% | **38문항·71청크** (+2문항·+5청크, 상한 내) | ✅ |
| b3 핵심 문항 유지 | g14·선물(g12)·법인카드(g09)·g13 | expected 적중 + rescue 출신 승격 문서 전건 보존 | ✅ |
| b4 무관-지목 무해 | 원시 기록 | g10 지목 — 승격 문서 진입 0 | ✅(기록) |
| b5 관련 무지목 | 발생 시 golden 적중 기록·회귀 정지 | **발생 0건** (관련 45 전문항 지목 유지) | ✅ |

부기(원시 기록, 판정 무영향):
- g50 은 P1 지목 → 재실측 무지목 (라우터 판단 변동). 무관 문항이므로 스킵
  편입은 P2′ 취지에 부합, 승격 문서 진입 0 확인.
- g09 최종 5위 한 석이 CREDO(rrf4) ↔ 징계기준(rrf3) 교대 — 양 런 모두
  지목(스킵 False, P2′ 무변경 경로)·상위 4석 동일로 **리랭커 동점 변동**.
  지시서 기준(expected + rescue 출신 승격 문서 rrf16→4위)은 양 런 보존.

## 5. 게이트 (c) — negative 거절 회귀 ✅

`eval/neg_reject_run.py` — 운영 경로 직배선(retriever+synthesize+verify,
v1_sidecar 미경유로 NEXUS_CHAT_MODEL 오염 회피), 합성 = 운영 기본
gemini-3.7-flash. 기준선(사전 고정): ab37 A런(채택 구성) 마커 5/6
(g51 완곡 거절 문면).

결과(neg_reject_20260817T131312.json): **마커 6/6** (기준선 5/6 대비 회귀
없음 — 오히려 g51 도 통과), 6문항 전건 `rerank_skipped=true`·에러 0·전건
action=pass·grade=UNVERIFIED. 판정 ✅.

## 6. 수용 기준 자가 점검

- diff = `compass_engine/v1_retriever.py`(조건부 스킵) + 테스트 + eval 러너/
  판정 스크립트(읽기 전용) — **그 외 운영 코드 0**.
- 재실측 JSON 은 P1 동일 스키마(동일 러너 재사용) — 무편집.
- v1 무변경 / DB 쓰기 0 (SELECT·RPC 읽기만) / secrets 값 출력 0 /
  안전 표면 3불변 무변경 / 예약석 add-only·cap8 불변식 유지(테스트 고정).

## 7. 발효 보류 해제

009 G3′ 미통과(g47 리랭크 오염)로 걸려 있던 **발효 보류를 해제한다** —
게이트 3조건 통과(§3·§4·§5). 잔존 리스크: 무관-지목형(g10·g50형)은
리랭크가 수행되나 재실측에서 승격 문서 진입 0 — 답변층 거절(negative 마커)
이 최종 방어선으로 동작함을 게이트 (c)로 확인.
